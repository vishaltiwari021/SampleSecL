//what is Dependencies and devDependencies?
//install nodemon --save-dev
//what is scripts?-> :we can write something automate it key:value
// what is building? ->: A build script is often used to compile, bundle, or prepare your project for production
//build": "echo Building project... && mkdir -p dist && cp src/* dist/""->
//echo Building project... → Prints a message.
//mkdir -p dist → Creates a dist folder if it doesn’t exist.
//cp src/* dist/ → Copies files from src to dist.


console.log("4");
console.log("6");
